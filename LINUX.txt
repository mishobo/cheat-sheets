-- disk space
> df -h

# Memory
> free -g

# cpu info
> lscpu

> colored terminal
> apt install fish

# removing unused packages
> sudo apt-get autoremove

-- delete a file
> rm -f fileName

# remove folder
> rm -rf <folderName>

-- command history
> history

-- list file permissions
> ls -lrt

-- display contents of a file
> cat docker-compose.yml

-- service status, start, restart, enable
> sudo systemctl restart docker

# list services
> systemctl --type=service --state=running [dead, exited, failed, inactive, or running]

# list enables services
> systemctl list-unit-files --state=enabled

# look at one service details
> systemctl status sshd

# enable service on start up
> systemctl enable docker

> sudo systemctl enable application .service.

-- disk space
> df -h

-- ssh to ubuntu vm
> ssh root@172.104.158.134
password: 

# add user to an app/service
> sudo usermod -aG docker ${USER}
> su - ${USER}
> groups
> sudo usermod -aG docker username